<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.EmployeeMapper">
    <insert id="insert" parameterType="com.sky.entity.Employee">
        insert into employee
        <trim prefix="(" suffix=")" suffixOverrides=",">
         id,password,status,username, name, phone, sex, id_number,create_time, update_time,create_user,update_user
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
         #{id},#{password},#{status},#{username}, #{name}, #{phone}, #{sex}, #{idNumber},#{createTime},#{updateTime},#{createUser},#{updateUser}
        </trim>
    </insert>
    <select id="list" resultType="com.sky.entity.Employee">
        select  * from  employee
                  <where>
                      <if test="name!=null and name!=''">
                          and name like concat('%',#{name},'%')
                      </if>
                  </where>
                  order by  id desc
    </select>
</mapper>
